spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testbrumdb
    username: brum
    password: brum
    
  jpa:    database-platform: org.hibernate.dialect.H2Dialect
  h2:    console:      enabled: true
