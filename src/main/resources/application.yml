server:
  port: 8081
  servlet:
    contextPath: /
spring:
  datasource:
    driverClassName: org.h2.Driver
    password: brum
    url: jdbc:h2:mem:brumdb
    username: brum
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
